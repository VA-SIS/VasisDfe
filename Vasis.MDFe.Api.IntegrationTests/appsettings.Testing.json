{
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "Microsoft.AspNetCore": "Warning",
      "Vasis.MDFe.Api": "Debug"
    }
  },
  "JwtSettings": {
    // IMPORTANTE: Esta chave deve ser a mesma chave (KEY) que sua API usa para assinar e validar tokens JWT.
    // Ela DEVE ter pelo menos 32 bytes para HS256. Adapte o valor para a chave real da sua aplicação.
    "Key": "EstaEUmaChaveSecretaQueDeveTerPeloMenos32BytesParaHS256",
    // O Issuer deve ser o mesmo que sua API espera.
    "Issuer": "Vasis.MDFe.Api.Test",
    // O Audience deve ser o mesmo que sua API espera.
    "Audience": "Vasis.MDFe.Api.Clients.Test"
  }
}